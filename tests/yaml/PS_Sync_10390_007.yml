valuesFilePath: ./values/helm/values.yml

pipelines:
{{- with .Values.repoBranch }}
  - name: P_flow
    steps:
      - name: S_flow
        type: Bash
        configuration:
          environmentVariables:
            env1: gitBranch
            env3: {{ genCA "foo-ca" 365 }}
        execution:
          onExecute:
            - echo {{ genCA "foo-ca" 365 }}
            - echo {{ sha256sum 'master' }}
{{ end }}  
