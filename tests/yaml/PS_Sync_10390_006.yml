valuesFilePath: ./values/helm/values.yml

pipelines:
  {{- range .Values.repoBranch }}
  - name: P_data
    steps:
      - name: S_data
        type: Bash
        configuration:
          environmentVariables:
            env1: gitBranch
            env2: sourceRepository
        execution:
          onExecute:
            - echo {{ append  .Values.repoBranch 5 }}
            - echo {{ until 5 }}
            - echo {{ max 1 2 5 }}
            - echo {{ base "foo/bar/baz" }}
