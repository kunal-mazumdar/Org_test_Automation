resources:
  - name: myPropertyBag
    type: PropertyBag
    configuration:
      commitSha: 1
      runID: 1
  - name: myFirstRepo
    type: GitRepo
    configuration:
      # SCM integration where the repository is located
      gitProvider: s_gitHub # this will be replaced from values.yml
      # Repository path, including org name/repo name
      path: nitind123/Org_test_Automation # this will be replaced from values.yml

pipelines:
  - name: demo_manual

    steps:
      - name: manual_trigger
        type: Bash
        configuration:
          inputResources:
            - name: myPropertyBag
        execution:
          onExecute:
            - printenv
            - echo "hi"
      - name: manual_trigger1
        type: Bash
        configuration:
          inputSteps:
            - name: manual_trigger
          inputResources:
            - name: myFirstRepo
        execution:
          onExecute:
            - printenv
            - echo "hi"
